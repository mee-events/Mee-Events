# Public browser variables only. Never put backend secrets here.
# Staging/production builds require https and a non-loopback API host.
NEXT_PUBLIC_APP_ENV=staging
NEXT_PUBLIC_API_BASE_URL=https://staging-api.example.com/api/v1
