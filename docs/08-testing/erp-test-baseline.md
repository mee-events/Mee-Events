# ERP Test Baseline — STAB-08

- **Verified:** 26 August 2026 00:40 IST (Asia/Kolkata, +0530)
- **Repository:** `/Users/vinaychilagani/Desktop/Mee Event V1`
- **Branch / starting commit:** `master` / `77f71a697fc6140f22e1d724b3e551a4a970e2b9`
- **Task:** STAB-08 — ERP tests
- **Result:** **PASS WITH DOCUMENTED GAPS** — 3/3 files and 8/8 tests pass; zero failures, skips, or todos. Zero-test discovery now fails closed.
- **Scope boundary:** Node-based unit tests for the environment reader, one employee API refresh path, and one catalogue form helper. This is not component rendering, browser E2E, backend integration, authorization, or production-readiness proof.

## Runner and discovery contract

| Field                     | Verified value                                                                                                                                                                  |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace                 | `@me-event/erp-web` in `apps/erp-web`                                                                                                                                           |
| Runner                    | Vitest `3.2.7` on Node `v20.20.2` (`darwin-arm64`)                                                                                                                              |
| Package script before     | `test: vitest run --passWithNoTests`                                                                                                                                            |
| Package script after      | `test: vitest run`                                                                                                                                                              |
| Canonical command         | `corepack pnpm --filter @me-event/erp-web test`                                                                                                                                 |
| Configuration source      | No `vitest.config.*`, `vite.config.*`, workspace file, or setup file exists; Vitest 3.2.7 defaults apply from `apps/erp-web`                                                    |
| Discovery include         | `**/*.{test,spec}.?(c\|m)[jt]s?(x)`                                                                                                                                             |
| Discovery exclusions      | Vitest defaults, including `node_modules`, `dist`, `cypress`, hidden cache/output/temp folders, and tool configuration files                                                    |
| Environment / pool        | Node environment; forks pool. Node 20 supplies `fetch` and `Response`; the API test supplies its own storage stub. No DOM emulator is installed or selected                     |
| Isolation / parallelism   | Per-file isolation enabled; file parallelism enabled for the canonical run                                                                                                      |
| Timeouts                  | Test 5,000 ms; hook 10,000 ms; teardown 10,000 ms                                                                                                                               |
| Retry / bail              | Retry 0; bail 0                                                                                                                                                                 |
| Focused tests             | No `.only`; CI also makes Vitest `allowOnly` false                                                                                                                              |
| Skipped/conditional tests | No `.skip`, `.todo`, `skipIf`, or `runIf`                                                                                                                                       |
| Setup / teardown          | No global setup. The API test creates fresh memory storage in `beforeEach` and restores all stubbed globals/mocks in `afterEach`; environment tests do not mutate `process.env` |
| Snapshots                 | None                                                                                                                                                                            |
| Coverage                  | Not configured or measured. No direct coverage dependency, script, threshold, or CI report exists                                                                               |
| Browser/component tooling | No direct Playwright, Cypress, React Testing Library, jsdom, happy-dom, or Vitest browser dependency/configuration                                                              |
| CI entry point            | Root `pnpm test` recursively invokes the ERP script in the TypeScript CI job; no browser, backend, PostgreSQL, or Redis service is started for it                               |

### Fail-closed discovery correction

| Probe             | Command                                                                      | Result                                                                                   |
| ----------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Before correction | `corepack pnpm --filter @me-event/erp-web test __stab08_no_such_test_file__` | `No test files found`, exit **0** because `--passWithNoTests` was active — **FAIL OPEN** |
| After correction  | Same command after removing the option                                       | `No test files found`, exit **1** — **FAIL CLOSED**                                      |

No real test was renamed or removed for either probe, and no alternative bypass was added.

## Execution evidence

| Run                               | Command                                                                                                                                                   | Files | Tests | Fail / skip / todo | Duration | Exit |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----: | ----: | ------------------ | -------: | ---: |
| Canonical after script correction | `corepack pnpm --filter @me-event/erp-web test`                                                                                                           |   3/3 |   8/8 | 0 / 0 / 0          |   330 ms |    0 |
| Isolation / order challenge       | `corepack pnpm --filter @me-event/erp-web exec vitest run --sequence.shuffle --sequence.seed=6082026 --maxWorkers=1 --minWorkers=1 --no-file-parallelism` |   3/3 |   8/8 | 0 / 0 / 0          |   429 ms |    0 |

The challenge run used a fresh process, shuffled files and tests with the recorded seed, forced one worker, and disabled file parallelism. Counts and outcomes matched. There were no teardown, unhandled-error, console, or leaked-global warnings.

## Existing test inventory and assertion review

| Test file                                      | Tests | Production behavior exercised                                             | Mocks / global state                                                                              | Positive / negative evidence                                                                                                                                                        | Important limit                                                                                                                                               | Strength                                     |
| ---------------------------------------------- | ----: | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| `src/lib/environment.spec.ts`                  |     6 | `resolveEmployeeAppEnv` and `resolveEmployeeApiBaseUrl`                   | None; explicit environment objects only                                                           | Development default and explicit URL; valid production HTTPS; missing production URL, HTTPS, loopback, and staging HTTP denial                                                      | No malformed URL case, alternative loopback notation, credential-bearing URL, or path-policy requirement                                                      | **Meaningful narrow unit coverage**          |
| `src/lib/employee-api.spec.ts`                 |     1 | Actual protected-request, refresh, stored-session update, and retry logic | Stubbed `window.sessionStorage` and `fetch`, both restored after each test; synthetic tokens only | First lead request uses old access token and returns 401; refresh sends current refresh token; storage rotates; retried lead request uses new access token and returns the response | No refresh failure, missing token, malformed JSON, repeated 401, logout, concurrency, network failure, capability/role denial, or runtime response validation | **Useful happy-path regression**             |
| `src/app/catalog/catalog-review-panel.spec.ts` |     1 | Exported production `blankCatalogMediaForm` helper                        | Local filled fixture only; no globals                                                             | Every form field is asserted to reset to its intended new-cover value and differ from the selected record                                                                           | Does not render React, call APIs, verify capabilities/uploads/images/provenance, or exercise browser behavior                                                 | **Strong helper unit; not a component test** |

Two narrow corrections were made without changing the eight-test count:

1. The localhost production test now uses `https://localhost` and expects the loopback error. Previously `http://localhost` stopped at the HTTPS check and did not prove its stated behavior.
2. The refresh test now asserts request endpoint order, old/new bearer headers, and the refresh-token request body in addition to result and stored-token rotation.

Every registered test contains meaningful assertions. There are no empty tests, always-true assertions, swallowed test errors, arbitrary waits, retries, snapshots, or conditional early returns.

## Maintained ERP surface inventory

The maintained TypeScript surface contains 62 `.ts`/`.tsx` files: 44 App Router `page.tsx` routes, the root layout, 10 supporting route/component modules, four production library modules, and three specs. All were inspected. The stylesheet was also inspected. The route count corrects the older audit claim of 45 pages.

Supporting modules:

- Leads: types, fixture data, card, detail panel, Kanban board, and toolbar.
- Catalogue: review panel and blank-form helper.
- Route clients: finance events, operations events, and vendor assignments.
- Libraries: environment reader, employee API/session client, employee bootstrap reader, and contract re-export.

### Route/component coverage and risk map

`Unit` below means a directly relevant unit test, not a test of the rendered route. Component and browser columns are zero across the entire surface.

| Route/module                            | Routes                                                                                                                                                    | Data source / status                                                                                      | Auth and capability posture                                                                                      | Unit                   | Component | Browser E2E | Primary risk                                                                                                                                       | Follow-up                                   |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------- | --------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| Dashboard                               | `/`                                                                                                                                                       | Static arrays; openly labeled illustrative sample, `SYNC OFF`, local foundation                           | Publicly rendered; no employee gate                                                                              | None                   | None      | None        | Static business counts, approval queue, profile, and role claims can be mistaken for operations evidence despite labels                            | CRM-04, ERP-01, STAB-18, STAB-17            |
| Login                                   | `/login`                                                                                                                                                  | Real OTP API client                                                                                       | Public entry; successful OTP goes directly to `/leads`; employee bootstrap/capability eligibility is not checked | Environment only       | None      | None        | Customer/vendor/worker account can reach the employee shell before backend APIs deny data; no login/failure/browser proof                          | CRM-01–03, CRM-24, STAB-17, STAB-20, CRM-26 |
| Leads / enquiries                       | `/leads`, `/leads/[id]`                                                                                                                                   | List/Kanban uses eight `DUMMY_LEADS`; detail uses real CRM APIs                                           | Client `sessionStorage` redirect; server remains authoritative; no client capability gate                        | None                   | None      | None        | Fixture PII-like records, local-only status edits, and inconsistent fixture/live detail paths                                                      | CRM-06, CRM-10–11, CRM-19–24, CRM-26        |
| Commercial handoff                      | `/quotes`, `/quotes/[id]`, `/bookings/[id]`, `/events`, `/events/[id]`                                                                                    | Real API calls with scaffold values in create/confirm actions                                             | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Money coercion, fixed draft values, manual confirmation, state transitions, polling, error handling, and cross-resource IDs are unproved           | CRM-12–15, CRM-24–26, STAB-17, STAB-20      |
| Catalogue                               | `/catalog` plus review panel                                                                                                                              | Real public catalogue fetches and protected review APIs; fetch failures become empty arrays plus a banner | Public taxonomy; review relies on stored session and backend capabilities                                        | Blank-form helper only | None      | None        | Helper test does not cover rendering, approval/provenance rules, arbitrary media URLs, API failures, or authorization                              | CUST-07/08, STAB-17, STAB-20                |
| Manager operations                      | `/manager`, `/manager/events/[id]`, `/manager/tasks/[id]`                                                                                                 | Real manager/event/task APIs                                                                              | Client session redirect; backend capability/assignment policy only                                               | None                   | None      | None        | Assignment, task status, comments, branch scope, stale writes, and direct-route denial are unproved                                                | ERP-02–04, ERP-20–22                        |
| Vendor administration                   | `/vendors`, `/vendors/new`, `/vendors/[id]`, `/vendors/assignments`, `/vendors/assignments/[id]`                                                          | Real vendor/category/event/assignment APIs                                                                | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Vendor PII/bank data, cross-vendor/branch access, state transitions, and assignment integrity have no web regression proof                         | ERP-05, ERP-15, ERP-20–22                   |
| Worker administration                   | `/workers`, `/workers/new`, `/workers/[id]`, `/workers/tasks`, `/workers/attendance`                                                                      | Real worker/vendor/event/task/attendance APIs                                                             | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Worker PII, vendor relationship, attendance, assignment, and cross-branch denial are unproved                                                      | ERP-06, ERP-20–22                           |
| Inventory / warehouse                   | `/inventory`, `/inventory/[id]`, `/inventory/allocations`, `/inventory/movements`, `/inventory/maintenance`, `/warehouse`                                 | Real inventory/warehouse/event APIs                                                                       | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Stock transitions, quantities, concurrent allocation, branch scope, custody, and maintenance errors are unproved                                   | ERP-07–09, ERP-20–22                        |
| Operations                              | `/operations`, `/operations/events`, `/operations/tasks`, `/operations/attendance`, `/operations/issues`, `/operations/materials`, `/operations/progress` | Real operations/event APIs                                                                                | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Mutations often lack local error handling; task/issue/material/completion authorization and state integrity are unproved                           | ERP-01–03, ERP-20–22                        |
| Finance                                 | `/finance`, `/finance/events`, `/finance/expenses`, `/finance/invoices`, `/finance/receipts`, `/finance/ledger`, `/finance/vendors`, `/finance/workers`   | Real finance/event/vendor/worker APIs with fixed scaffold action amounts                                  | Client session redirect; backend capabilities only                                                               | None                   | None      | None        | Money parsing, fixed amounts, approve/pay sequencing, reconciliation, segregation of duties, branch scope, and double-submit behavior are unproved | ERP-13–16, ERP-20–22, INT-02                |
| Customer 360 / approval inbox / reports | No implemented routes; dashboard hash links/sample cards only                                                                                             | Missing or illustrative                                                                                   | No enforceable route behavior                                                                                    | None                   | None      | None        | Product surface can imply capability without implementation or proof                                                                               | CRM-10/13/18, ERP-16/18, STAB-18            |
| Search/filter/pagination and states     | Lead search/filter is client-only over fixtures; most API lists are unbounded in the UI                                                                   | Mixed scaffold/real                                                                                       | No specific policy test                                                                                          | None                   | None      | None        | No server pagination/filter tests, URL-state tests, large-list behavior, or systematic loading/error/empty-state rendering tests                   | CRM-19–21, STAB-17, CRM-26/ERP-22           |

## Fixture and scaffold findings

| File / route                                       | Finding                                                                                                                   | Visible and labeled?                                                            | Security/privacy impact                                                                                                                        | Product impact                                                                               | Follow-up                       |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------- |
| `src/components/leads/dummy-leads.ts` → `/leads`   | Eight hard-coded realistic names, Indian mobile numbers, events, dates, owners, and references drive the production route | Visible; **not labeled as demo on the leads screen**                            | No evidence the values are real, but realistic PII-shaped data without provenance or an in-screen fixture label creates privacy/confusion risk | **High:** status edits are local only and the live `listLeads` client is unused by the board | CRM-06, CRM-24, CRM-26, STAB-18 |
| `src/app/page.tsx` → `/`                           | Static metrics, approvals, profile name, roles, dates, and operational claims                                             | Clearly labeled illustrative sample, `SYNC OFF`, and “No live customer records” | Low privacy risk because it is labeled, though realistic identities/reference codes remain visible                                             | Medium: can still look like an operational dashboard                                         | CRM-04, ERP-01, STAB-18         |
| `src/app/leads/[id]/page.tsx`                      | New quotation action injects a ₹25,000 starter package, 18% GST, 30% advance, and fixed terms/notes                       | Not labeled as a demo preset                                                    | Medium integrity risk if an operator accepts defaults without review                                                                           | High for quotation correctness                                                               | CRM-12, CRM-24, CRM-26          |
| `src/app/finance/events/finance-events-client.tsx` | Default ₹10,000 UPI payment and fixed ₹1,000 “Ops expense” actions                                                        | Not labeled as scaffold                                                         | High financial-integrity/authorization risk if used operationally                                                                              | High                                                                                         | ERP-13/14/20/22, INT-02         |
| `src/app/finance/invoices/page.tsx`                | Button issues a fixed ₹50,000 invoice                                                                                     | Not labeled as scaffold                                                         | High financial-integrity risk                                                                                                                  | High                                                                                         | ERP-13/16/20/22                 |
| `src/app/finance/vendors/page.tsx`                 | Button creates a fixed ₹5,000 vendor settlement                                                                           | Not labeled as scaffold                                                         | High settlement and segregation-of-duties risk                                                                                                 | High                                                                                         | ERP-15/16/20/22                 |
| `src/app/finance/workers/page.tsx`                 | Button creates a fixed ₹2,000 payout and then chains approval and payment                                                 | Not labeled as scaffold                                                         | High payout, double-submit, and segregation-of-duties risk                                                                                     | High                                                                                         | ERP-13/16/20/22                 |
| `src/app/events/[id]/page.tsx`                     | Venue save generates `maps://placeholder` from the entered address                                                        | Explicitly labeled placeholder                                                  | Venue information is still persisted/displayed; no maps-provider privacy policy exists                                                         | Medium                                                                                       | ERP-02, INT-07                  |
| `src/app/login/page.tsx`                           | Displays a local seed phone and any API-supplied `debugCode`                                                              | Explicitly labeled local development                                            | Backend production configuration is fail-closed, but the UI itself does not suppress debug codes by environment                                | Medium defense-in-depth gap                                                                  | CRM-01/24/26, STAB-20           |

STAB-08 documents these production-visible scaffolds; it does not remove or complete them.

## Security coverage map

| Area                              | Existing evidence                                                                                                                        | Missing evidence / risk                                                                                                                                        | Severity                                 | Follow-up owner                                  |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------ |
| Public environment                | Six direct tests prove development defaults and staging/production fail-closed URL rules, including a corrected HTTPS-loopback assertion | Malformed URL variants, additional loopback forms, credential-bearing URLs, and build/runtime boundary behavior                                                | Medium                                   | STAB-12, STAB-20                                 |
| Employee login / bootstrap        | OTP client and a strict `employee_web` bootstrap type guard exist                                                                        | No test or route uses the bootstrap gate after login; non-employee role routing and login failures are unproved                                                | High                                     | CRM-01–03, CRM-24/26, STAB-17/20                 |
| Session storage                   | One test uses isolated memory `sessionStorage`; real code stores access and refresh tokens in browser-readable session storage           | XSS can read tokens; stored JSON is only partially checked before a full type assertion; logout/invalid storage/restart behavior untested                      | High                                     | CRM-02/24/26, STAB-20                            |
| Refresh behavior                  | One strengthened happy-path test proves 401 → refresh → token update → authorized retry                                                  | Refresh failure, no token, malformed JSON, repeated 401, revoked token, concurrent callers, timeout, and clearing behavior have no test                        | High                                     | CRM-02/24/26, STAB-17/20                         |
| Route/capability/role enforcement | Backend is intended to be authoritative; no unsafe HTML insertion was found                                                              | No middleware/server route gate, component capability gate, direct-route denial, or ERP authorization test; existing bootstrap loader is unused                | High                                     | CRM-03/24/26, ERP-20/22, STAB-17/20              |
| Branch / IDOR / BOLA              | No ERP test evidence                                                                                                                     | Dynamic IDs span leads, quotes, events, manager, vendor, worker, inventory, operations, and finance; backend audit already records inconsistent branch scoping | High                                     | SEC-02/STAB-20, STAB-17, CRM-24, ERP-20          |
| XSS / HTML / CSP                  | React text rendering is used; no `dangerouslySetInnerHTML`, `innerHTML`, or token-in-URL/log pattern was found                           | No rendering tests for hostile API strings; no CSP; operator-supplied media URLs and backend error messages reach the UI                                       | High because tokens are browser-readable | SEC-05/STAB-20, CRM-24, ERP-20/22                |
| CSRF                              | Requests use explicit bearer headers and no cookie/credentialed fetch was found, reducing current ambient-auth CSRF exposure             | Assumption is untested and must be revisited if cookie auth is introduced                                                                                      | Informational                            | STAB-20 / future auth ADR                        |
| External/API JSON                 | TypeScript response types and API contracts exist                                                                                        | `response.json()` is asserted to generic types without runtime schema validation; malformed session/response behavior is untested                              | Medium                                   | CRM-24, ERP-20, shared contract/integration work |
| Error/loading/empty handling      | Many routes render local states and clear storage on some 401 paths                                                                      | No component/browser proof; handling is inconsistent and several mutation handlers omit local error boundaries                                                 | Medium                                   | STAB-17, CRM-26, ERP-22                          |
| Fixture privacy                   | Root dashboard is labeled; lead phone rendering is masked in the detail panel                                                            | Production lead board uses unlabeled realistic fixture identities and phone numbers                                                                            | Medium                                   | CRM-06/24/26, STAB-18                            |

TypeScript and React escaping are useful controls, but neither replaces runtime validation, backend authorization, browser security testing, or a Content Security Policy.

## Isolation, hidden-test, and secret review

- No `.only`, `.skip`, `.todo`, `skipIf`, `runIf`, `.concurrent`, retry, broad timeout, snapshot, conditional return, or ignored-unhandled-error option was found.
- Environment tests pass explicit objects and do not read or mutate the developer's `.env.local`.
- The API test uses synthetic token strings, replaces `window.sessionStorage` with a fresh in-memory implementation, stubs `fetch`, and restores globals/mocks in `afterEach`.
- Canonical and shuffled serialized runs returned identical counts without environment, fetch, teardown, or unhandled errors.
- No test starts Next.js, reaches the network, uses a real API endpoint, or contains an employee credential, OTP, or production token.
- No browser, component, coverage, snapshot, generated `.next`, or test-output artifact was created or tracked.

## Verification commands

- `corepack pnpm exec vitest --version`
- `corepack pnpm --filter @me-event/erp-web test __stab08_no_such_test_file__` before and after script correction
- `corepack pnpm --filter @me-event/erp-web test`
- `corepack pnpm --filter @me-event/erp-web exec vitest run --sequence.shuffle --sequence.seed=6082026 --maxWorkers=1 --minWorkers=1 --no-file-parallelism`
- `corepack pnpm --filter @me-event/erp-web lint`
- `corepack pnpm --filter @me-event/erp-web typecheck`
- `corepack pnpm format:check`
- `git diff --check`

## STAB-08 conclusion

The eight-test ERP suite is green, deterministic under a shuffled serialized fresh process, and now fails when discovery finds zero tests. Its three tests provide meaningful but extremely narrow unit evidence. They do not prove any rendered route, employee authorization, branch isolation, browser session lifecycle, real API integration, or CRM/ERP business journey.

The production-visible fixture and fixed-value behaviors remain explicitly assigned to later CRM/ERP/security blocks. Phase 0 remains **NOT PASSED**. The next permitted task is **STAB-09 — Flutter analysis**; STAB-09 was not started here.

## STAB-17 follow-up (27 August 2026)

STAB-17 added `apps/erp-web/vitest.config.ts` so Vitest excludes `e2e/`. The
STAB-08 unit command still passes 3/3 files and 8/8 tests. Playwright lives
under `apps/erp-web/e2e/` and is not part of `pnpm --filter @me-event/erp-web test`.
See [e2e-foundation-baseline.md](./e2e-foundation-baseline.md).
